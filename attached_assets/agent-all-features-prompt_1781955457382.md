None of the features I asked for in previous sessions are actually working in the live app right now, even though earlier work may have claimed they were done. I need you to verify each one against the real running app, not the code in isolation, and actually make each one work end to end before telling me it's finished. Go one feature at a time, test each in the live preview before moving to the next.

FEATURE 1 — Interactive UI elements in the chat instead of plain text
When the AI asks about work style, it should render an actual slider control inline in the chat (Structured on one end, Highly Adaptive on the other), not expect a typed answer. When it asks about tech stack, it should render clickable tag buttons for common stacks (JavaScript, TypeScript, Python, Go, React, Node.js, plus a way to add a custom one), allowing multi-select. When it asks about contract type, it should render multiple-choice buttons (Full-time, Part-time, Contractor) instead of a text field. Right now none of this exists, the chat is still plain text only for every question. Build these as real React components inside ChatModal.tsx, rendered conditionally based on which question the AI is currently asking, and confirm by actually running through the chat yourself that they appear and that tapping/selecting an option correctly sends that answer back into the conversation.

FEATURE 2 — Live "Candidate Spec Profile" sidebar
There should be a visible panel alongside the chat that updates in real time as the user answers questions, showing fields like Role, Tech Stack, Seniority, Timeline, and so on, populating one by one as each is established in conversation, with unfilled fields shown in a clear "not yet answered" state. Right now this does not exist anywhere in the UI. Build it as a real component, connect it to the actual conversation state so it updates live as messages come in, and confirm by running through the chat yourself that fields visibly populate as you answer.

FEATURE 3 — Voice to text via the microphone button
There is a microphone button in the chat that should let the user speak instead of typing, using the Web Speech API. Right now tapping it produces a permission error and does not work. Investigate the actual cause; check specifically whether SpeechRecognition.start() is being called synchronously within the click handler with no async code or delay before it, since that is the most common reason this exact kind of permission error happens. Fix it and confirm by actually testing voice input yourself that it requests permission correctly and transcribes speech into the input field.

FEATURE 4 — Candidate join form actually saves to the database
The "join our network" form should submit real data to a backend route and save it into the join_applications table in Supabase. Right now submitting it returns a server error instead of succeeding. Find out why the route isn't being reached correctly (check whether the frontend's fetch URL actually matches where the route is registered in the backend), fix it, then confirm by actually submitting a real test application and checking Supabase directly that a new row appears with the correct data.

FEATURE 5 — AI chat reliably responds without errors
The chat should never show a generic "something went wrong" error to the user unless the AI response itself genuinely failed. If this is currently happening because a Supabase logging or save operation is failing and incorrectly breaking the whole chat response, separate those two things: a failed database save should be logged silently on the server and never block or interrupt the user's actual conversation. Confirm by sending several real test messages in a row and confirming you get real AI responses every time, with no errors.

IMPORTANT — DO NOT MARK ANYTHING AS DONE WITHOUT REAL VERIFICATION
For every single feature above, after you believe it's implemented, actually use it yourself in the live running preview exactly as a real user would, and only report it as working if you've personally seen it work with your own test. If something doesn't work after your attempt, say so clearly and tell me what's still broken rather than reporting success. At the end, give me a clear list of which of these five features are now genuinely confirmed working, and which (if any) are still not working and why.
