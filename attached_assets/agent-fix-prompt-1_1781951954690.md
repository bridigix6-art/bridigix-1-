My app's AI chat isn't saving anything to the database, and I need you to find the actual root cause and fix it completely — not just patch the symptom.

CONTEXT:
- The chat lives at artifacts/api-server/src/routes/chat.ts and uses the Groq SDK to generate AI replies.
- The Groq API key was revoked once already because it was accidentally committed to .replit in plain text. I've since rotated it to a new key and added it to Replit Secrets as GROQ_API_KEY.
- I also have SUPABASE_URL and SUPABASE_ANON_KEY in Secrets, and three tables already exist in Supabase: chat_conversations, join_applications, and visitor_sessions, all created successfully and confirmed visible in the Supabase Table Editor.
- chat.ts was supposed to insert a row into chat_conversations after every message exchange, saving the full message array, but when I test the chat live, no rows ever appear in Supabase, and I'm not getting any visible errors in the chat UI itself.
- There's also a candidate "join our network" form on the site (JoinPage.tsx) that's supposed to save into join_applications, but right now its handleSubmit function only sets local state and never actually calls any backend route — there's no join.ts route file at all yet.

WHAT I NEED YOU TO DO:

1. First, verify the Groq API key actually works right now. Write and run a small isolated test that calls the Groq API directly using the GROQ_API_KEY secret, completely separate from the chat route, and show me the real result — success or the exact error message. Do not assume it works just because no error is visible in the UI.

2. Check whether chat.ts is actually importing and calling the Supabase client correctly, and whether the insert into chat_conversations is really being reached in the code path, or silently failing or being skipped. Add proper error logging if it isn't already there, then actually trigger a real test message through the live chat and show me what gets logged.

3. Check Supabase directly — confirm whether Row Level Security is enabled on any of the three tables in a way that would silently block inserts coming from the anon key used by the backend. If so, fix the policies properly (don't just disable RLS as a permanent solution — set up a policy that allows the backend to insert, ideally using the service role key server-side if that's more appropriate, and explain which approach you used and why).

4. Build the missing join.ts route from scratch. It should accept the exact fields currently in JoinPage.tsx's form state (name, email, location, role, otherRole, experience, skills, github, linkedin, project, environment, status, availability, workType, salary, notes), validate that name and email are present, and insert a real row into join_applications. Then update JoinPage.tsx's handleSubmit so it actually calls this new route via fetch before showing the "Application received" success screen, and properly handles a failure case instead of always showing success regardless of what happened.

5. Once both pieces are wired up, actually test the full flow yourself end to end: send a real test message through the live chat, submit a real test application through the join form, and confirm real rows appear in both chat_conversations and join_applications in Supabase. Show me proof this worked, not just that the code compiles without errors.

6. Before finishing, double check that no API keys, passwords, or other secrets are hardcoded anywhere in the codebase outside of Replit Secrets — especially in .replit, any config files, or anywhere they could end up committed to git again.

Please work through this methodically, verify each step actually works before moving to the next, and tell me clearly at the end exactly what was wrong and what you changed, so I understand it rather than just trusting it's fixed.
