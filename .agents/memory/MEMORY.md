- [Bridgix stack + routing](bridgix-project.md) — React+Vite+Tailwind v4+Framer Motion; wouter router with BASE_URL base; /join page for talent network.
- [Bridgix API](bridgix-api.md) — Express API server on port 8080; Groq SDK (llama-3.3-70b-versatile) via GROQ_API_KEY env var; chat route at /api/chat.
- [Supabase schema reality](supabase-schema.md) — join_applications has flat columns (not form_data JSONB); chat_conversations has no unique constraint on email so upsert fails; use select+update/insert pattern for save-chat. Newsletter emails saved to join_applications with status="newsletter".
- [JoinPage back button](bridgix-project.md) — use window.location.href = import.meta.env.BASE_URL || "/" instead of wouter navigate("/") for reliable back navigation due to base path handling.
- [Bridgix chat architecture](bridgix-chat.md) — chat.ts uses sessionId (from frontend) for select+update/insert to avoid duplicate DB rows; parseIntakeComplete extracts 11 fields from INTAKE_COMPLETE block; review phase sits between INTAKE_COMPLETE detection and CompletionPanel; hiring_briefs table may not exist yet (graceful fallback to chat_conversations.intake_summary).
- [Bridgix interactive widget stacking fix](bridgix-chat.md) — isLastAI must be ONLY `i === messages.length - 1`; the old second condition (second-to-last when last is also AI) caused double widgets. interactiveUsed Set is reset on modal close; this is fine because isLastMsg now prevents re-showing old interactive on restored sessions.
- [Bridgix sidebar spec extraction](bridgix-chat.md) — client-side extractSpec (regex keyword matching) replaced with /api/extract-spec endpoint using llama-3.1-8b-instant; fetchLiveSpec() called fire-and-forget after each AI reply; liveSpec state reset on startFresh and modal close. Never revert to client-side regex.
- [Bridgix SYSTEM_PROMPT closing gate](bridgix-chat.md) — 20-field numbered checklist in CLOSING GATE section; model must confirm all 20 YES before outputting render_component signal; tested: AI correctly continued collecting fields [17]-[20] after covering [1]-[16] without early close.
- [Bridgix RecoveryBar completed-conversation](bridgix-chat.md) — load-chat returns intakeSummary when status=complete; RecoveryBar checks for intakeSummary and shows "already_complete" state instead of restoring messages as in-progress. Label changed to "Resume a previous intake?" (not "completed intake").
- [Bridgix SYSTEM_PROMPT](bridgix-chat.md) — full clean rewrite done: removed junk text, removed duplicate MANDATORY PILLARS section, all 13 required field groups now explicit including min years experience, reporting structure with name/title, must-have vs nice-to-have as separate questions for both tech AND culture, red flags, start date/urgency, prior hiring history. INTAKE_COMPLETE format updated to include REPORTING STRUCTURE field.
