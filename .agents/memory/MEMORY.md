- [Bridgix stack + routing](bridgix-project.md) — React+Vite+Tailwind v4+Framer Motion; wouter router with BASE_URL base; /join page for talent network.
- [Bridgix API](bridgix-api.md) — Express API server on port 8080; Gemini SDK (@google/generative-ai) via GEMINI_API_KEY env var; chat route at /api/chat; main model gemini-2.5-flash, extract-spec model gemini-2.0-flash.
- [Supabase schema reality](supabase-schema.md) — join_applications has flat columns (not form_data JSONB); chat_conversations has no unique constraint on email so upsert fails; use select+update/insert pattern for save-chat. Newsletter emails saved to join_applications with status="newsletter".
- [JoinPage back button](bridgix-project.md) — use window.location.href = import.meta.env.BASE_URL || "/" instead of wouter navigate("/") for reliable back navigation due to base path handling.
- [Bridgix chat architecture](bridgix-chat.md) — chat.ts uses sessionId (from frontend) for select+update/insert to avoid duplicate DB rows; parseIntakeComplete extracts 23 fields from INTAKE_COMPLETE block; review phase sits between INTAKE_COMPLETE detection and CompletionPanel.
- [Bridgix interactive widget lifecycle](bridgix-chat.md) — use `activeWidgetIndex: number|null` state for slider/tags/choice widgets; cleared at top of sendMessage AND in handleInteractiveConfirm; set inside setMessages() updater for new reply via detectInteractiveType(reply). contactFormIndex still uses interactiveUsed Set separately. Do NOT revert to isLastMsg+interactiveUsed approach — that caused widget to re-appear because detectInteractiveType matched follow-up AI messages that echoed keywords.
- [Bridgix sidebar spec extraction](bridgix-chat.md) — /api/extract-spec uses gemini-2.0-flash; fetchLiveSpec() called fire-and-forget after each AI reply; liveSpec state reset on startFresh and modal close. Never revert to client-side regex.
- [Bridgix Supabase Node20 fix](bridgix-api.md) — CRITICAL: supabase.ts MUST import WebSocket from "ws" and pass { realtime: { transport: WebSocket } } to both createClient calls. ws package is NOT a groq-sdk dependency — must be explicitly listed in api-server/package.json. If ws is removed, the API server crashes at startup with a Node 20 WebSocket error.
- [Bridgix vite proxy](bridgix-project.md) — Vite dev server MUST have proxy: { "/api": { target: "http://localhost:8080" } } in vite.config.ts or all /api fetch calls fail silently.
- [Bridgix DB tables](bridgix-chat.md) — sessions/chat_messages/session_state/session_exports tables created via raw psql from lib/db/migrations/0000_create_sessions_tables.sql; pnpm --filter db push fails non-interactively.
- [Bridgix SYSTEM_PROMPT v3](bridgix-chat.md) — CLOSING GATE section triggers render_component: contact_info_form_bar; INTAKE_COMPLETE block has 23 fields; reflection cadence rule bans filler phrases. Gemini error handling: check message for "429"/"RESOURCE_EXHAUSTED" (rate), "403"/"API_KEY_INVALID"/"PERMISSION_DENIED" (auth), "400"/"INVALID_ARGUMENT" (bad request).
- [Bridgix RecoveryBar completed-conversation](bridgix-chat.md) — load-chat returns intakeSummary when status=complete; RecoveryBar shows "already_complete" state instead of restoring messages.
- [Bridgix Storage Architecture](bridgix-storage.md) — conversation-records bucket; completed_intakes table (raw SQL only, not supabase-js); screenshot at INTAKE_COMPLETE, PDF at brief confirm; admin panel shows both.
